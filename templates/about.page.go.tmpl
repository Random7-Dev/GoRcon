{{template "base" .}}
{{define "content"}}
<div class="container">
    <div class="row">
        <div class="col">
            <h1>This is the About</h1>
            <p> About page with styling.</p>
            <p>{{index .StringMap "test"}}</p>
        </div>
    </div>
</div>
{{end}}